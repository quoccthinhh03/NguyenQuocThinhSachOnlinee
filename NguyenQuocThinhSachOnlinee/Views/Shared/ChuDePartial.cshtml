@model  IEnumerable<NguyenQuocThinhSachOnlinee.Models.CHUDE>

<ul class="list-group">
    <li class="list-group-item active" style="font-weight:bold;text-align:center ">

        Sách Theo Chủ Đề
    </li>
    @foreach (var item in Model)
    {
        @Html.ActionLink(item.TenChuDe,"SachTheoChuDe", "TrangChu", new { id =item.MaCD }, new { @class="list-group-item"});
    }

</ul>